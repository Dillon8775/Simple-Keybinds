{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}+${mc_version}",
  "name": "${mod_name}",
  "accessWidener": "simplekeybinds.accesswidener",
  "description": "${description}",
  "authors": [
    "Dillon8775"
  ],
  "contact": {
    "homepage": "${homepage}",
    "sources": "${sources}",
    "issues": "${issues}"
  },
  "license": "${license}",
  "icon": "${logo}.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "net.dillon.simplekeybinds.SimpleKeybinds"
    ],
    "modmenu": [
      "net.dillon.simplekeybinds.impl.ModMenuImpl"
    ]
  },
  "mixins": [
    "simplekeybinds.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": ">=26.2-rc.1",
    "java": ">=${java_version}",
    "fabric-api": "*",
    "balm-fabric": ">=${balm_version}",
    "dillonlib": ">=${dillonlib_version}"
  },
  "suggests": {
    "modmenu": "*",
    "controlling": "*"
  },
  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "${discord}"
      }
    }
  }
}