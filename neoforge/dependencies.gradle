dependencies {
    implementation("net.blay09.mods:balm-neoforge:${balm_version}") {
        changing = balm_version.contains('SNAPSHOT')
    }

    implementation "maven.modrinth:dillon-lib:${project.mc_version}-${project.dillonlib_version}-neoforge"
}